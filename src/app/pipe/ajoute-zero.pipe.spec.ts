import { AjouteZeroPipe } from './ajoute-zero.pipe';

describe('AjouteZeroPipe', () => {
  it('create an instance', () => {
    const pipe = new AjouteZeroPipe();
    expect(pipe).toBeTruthy();
  });
});
